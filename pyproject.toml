[tool.poetry]
name = "quantumfetcher"
version = "0.1.0"
description = ""
authors = ["Marek Grzyb <marek@grzyb.dev>"]
readme = "README.md"

[tool.poetry.scripts]
quantumfetcher = "quantumfetcher.main:app"

[tool.poetry.dependencies]
python = "^3.13"
typer = "^0.15.0"
tqdm = "^4.67.1"
requests = "^2.32.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
